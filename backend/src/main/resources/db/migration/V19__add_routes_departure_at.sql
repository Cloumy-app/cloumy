-- ============================================================
-- V19: routes.departure_at 컬럼 신설
-- ============================================================
BEGIN;

-- 출국 일시(선택 입력) — 프로액티브 출국 준비 알림(T1)의 기준값.
-- NULL 허용: 미입력 시 T1만 동작 안 하고 나머지 규칙은 정상.
ALTER TABLE routes ADD COLUMN departure_at TIMESTAMPTZ;

COMMIT;
