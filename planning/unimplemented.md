# 미구현 목록 / 추후 처리 필요 항목

> 구현 중 발견된 임시 처리 항목, 프로덕션 전 반드시 완료해야 하는 작업 목록.
> 완료 시 해당 항목 삭제 또는 ~~취소선~~ 처리.

---

## 🔴 프로덕션 전 필수

### 1. Apple identity token 서명 검증 미구현
- **파일**: `backend/src/main/java/com/cloumy/auth/oauth/AppleOAuthClient.java`
- **현재 상태**: identity token(JWT)의 서명을 검증하지 않고 Base64 payload만 디코딩해서 사용
- **위험**: 위조된 identity token으로 타인 계정 탈취 가능
- **해야 할 것**: Apple 공개키 JWK endpoint(`https://appleid.apple.com/auth/keys`)에서 공개키를 가져와 RS256 서명 검증 추가
- **참고**: JJWT 또는 `com.nimbusds:nimbus-jose-jwt` 라이브러리 사용 권장

### 2. JWT 블랙리스트 조회가 Redis 장애 시 처리되지 않은 예외를 던짐
- **파일**: `backend/src/main/java/com/cloumy/auth/security/JwtTokenProvider.java` (validateToken, `redisTemplate.hasKey(BLACKLIST_KEY_PREFIX + ...)` 부분)
- **관련 태스크**: Rate Limiting 튜닝(Spring, 2026-07-04) 검증 중 발견 — `RateLimitFilter`의 fail-open 동작을 확인하려고 Redis 컨테이너를 잠깐 내렸더니, 내 필터에 도달하기도 전에 `JwtAuthenticationFilter`가 블랙리스트 조회에서 `RedisException`을 던져 500으로 응답함(정상 토큰인데도)
- **위험**: Redis 장애 시 인증이 필요한 모든 엔드포인트가 500을 반환 — 정상 사용자도 전부 막힘. 레이트리밋은 fail-open으로 설계했는데 그 앞단인 인증 자체가 fail-closed(사실상 의도치 않은 예외)라 전체 취지가 무색해짐
- **해야 할 것(논의 필요)**: 블랙리스트 조회를 try-catch로 감싸고 Redis 장애 시 정책 결정 필요 — "차단된 토큰인지 확인 불가 시 통과시킬지(로그아웃 처리가 늦게 반영되는 정도의 리스크)" vs "명시적으로 503 반환할지"(현재처럼 이유 불명확한 500보다는 나음)
- **우선순위**: 중간 — Redis 자체가 자주 죽는 컴포넌트는 아니지만, 장애 시 파급 범위가 이 기능 하나가 아니라 전체 인증이라 영향도가 큼

---

## 🟠 테스트 미완료

### 2. JWT 인증 흐름 엔드-투-엔드 테스트 미완료
- **관련 태스크**: JWT 인증 미들웨어 구현
- **현재 상태**: 코드 구현은 완료, 실제 서버 구동 후 테스트 미진행
- **테스트해야 할 흐름**:
  1. `POST /v1/auth/social` → Access/Refresh Token 발급 확인
  2. 발급된 Access Token으로 보호된 API 호출 → 200 확인
  3. 만료된 Access Token 사용 → 401 `TOKEN_EXPIRED` 확인
  4. `POST /v1/auth/refresh` → 새 Access Token 발급 확인
  5. `POST /v1/auth/logout` → Redis 블랙리스트 등록 확인
  6. 로그아웃된 Refresh Token으로 갱신 시도 → 401 `TOKEN_REVOKED` 확인

### (번호추가) 챗봇 카드 "바로 추가" 흐름 UI 미검증
- **관련 태스크**: 대화로 자리를 명시하면 확인 없이 바로 추가 (2026-07-30)
- **파일**: `frontend/app/(tabs)/chat.tsx` (`PlaceCardList`), `frontend/lib/i18n/locales/*.json`
- **현재 상태**: 서버 쪽은 실제 호출로 확인됐다 — 「고척 스카이돔 가기 전에 카페」 → `source: "conversation"`, 앵커 `1-0 한강레져스포츠`(고척 스카이돔이 `1-1`이라 정확), 「2일차에 카페」 → `source: "conversation_day"`, 「근처 카페」 → `source: "estimated"`. AI 170 passed / `tsc` 소스 에러 0 / Spring `compileJava` EXIT=0. 앱도 시뮬레이터에서 새 번들로 정상 기동했다.
- **못 한 것**: **챗봇 탭에 메시지를 타이핑하는 UI 조작을 자동화할 수단이 없어** 카드를 실제로 눌러보지 못했다. 즉 분기 로직은 서버 응답으로 증명됐지만 **탭 후 동작과 예고 문구 렌더는 눈으로 확인되지 않았다.**
- **테스트해야 할 흐름**:
  1. 「고척 스카이돔 가기 전에 카페 들르고 싶어」 → 카드 위 예고가 **「1일차 · 한강레져스포츠」 다음에 바로 추가돼요** 인지
  2. 그 카드 탭 → **시트가 안 뜨고** 바로 체크 표시로 바뀌는지 → 루트 화면에서 고척 스카이돔 **앞**에 들어갔는지
  3. 「2일차에 카페 추천해줘」 → 탭 → **시트가 뜨는지**, 문구가 "말씀하신 대로 …"가 아니라 **"2일차 마지막에 추가할까요?"** 인지 (기존 오문구 수정 확인)
  4. 「근처 카페 추천해줘」 → 탭 → 시트 (회귀 없음)
  5. 첫 일정 앞 삽입(앵커 null) → 예고가 **「N일차」 맨 앞에** 인지
  6. 4개 언어 전환 후 예고 문구가 깨지지 않는지
- **우선순위**: 🟠 머지 전 필수 — 코드는 있으나 UI 검증이 안 된 상태다

### (번호추가) Android 항공편 피커 실기기 검증 미완료
- **관련 태스크**: 프로액티브 후속 수정 — 항공편 시각 정합성 + 활성 루트 선택 (2026-07-29)
- **파일**: `frontend/app/route/[routeId]/index.tsx` (`FlightTimePicker`)
- **현재 상태**: 서버 검증 11종은 전부 통과했으나(AI 126 passed / Spring `compileJava` EXIT=0 / FE `tsc` 에러 0), **Android 실기기·에뮬레이터가 없어 피커 수정을 눌러보지 못했다.** 이번 수정의 상당 부분이 Android 피커 관련이다(2단계 분리 · `onDismiss` · 정오 정규화).
- **위험**: 정오 정규화와 `onDismiss`는 **`@expo/ui` 라이브러리 소스(JS·Kotlin)에서 근거를 찾은 것이지 실제로 재현·확인한 게 아니다.** iOS는 기존 `<Modal>` + spinner 경로를 그대로 둬서 영향이 없다.
- **테스트해야 할 흐름** (Android 실기기 필수):
  1. 가는 편 피커 열기 → 날짜·시각 **2단계**로 뜨고 다이얼로그가 겹치지 않는지
  2. 날짜 단계에서 뒤로가기/바깥 탭 → 다이얼로그가 닫히고 값이 안 바뀌는지 (FFE #6)
  3. 새벽 시각(02:00) 저장 → 재진입 → 피커 열기 → 날짜가 **하루 밀리지 않는지** (FFE #7)
  4. 지우기 → 뒤로 → 재진입 → 값이 되살아나지 않는지 (FFE #11)
  5. 재진입 후 피커 열기 → 초기값이 현재 시각이 아니라 **저장된 값**인지
  6. 진행 중 여행을 목록 맨 아래로 드래그 → 홈·챗봇이 **같은 진행 중 여행** 기준인지 (활성 루트)
- **우선순위**: 🟠 머지 전 필수 — 코드는 있으나 검증이 안 된 상태다

---

## 🟡 개발 환경 복원 필요

### 2. PostgreSQL 포트 5433 → 5432 복원
- **원인**: 다른 프로젝트(fintech_db) 실습 중 5432 포트 충돌로 임시 변경
- **수정할 파일**:
  - `docker-compose.yml` → `5433:5432` → `5432:5432`
  - `.env` → `localhost:5433` → `localhost:5432`
  - `.env.example` → `localhost:5433` → `localhost:5432`
- **복원 후**: `docker compose down && docker compose up -d` 실행

---

---

## 🔵 API 키 미발급 (시기별 등록 필요)

> 발급 후 `.env`, `ai/.env`, GitHub Secrets 세 곳에 동일하게 등록할 것.

### 3. 토스페이먼츠 시크릿 키 (`TOSS_PAYMENTS_SECRET_KEY`)
- **등록 시기**: Week 15~16 결제 구현 시
- **발급처**: developers.tosspayments.com → 개발 → 테스트 시크릿 키
- **등록 위치**: `.env` + GitHub Secrets

### 4. Apple 로그인 키 4종 (`APPLE_CLIENT_ID / APPLE_TEAM_ID / APPLE_KEY_ID / APPLE_PRIVATE_KEY`)
- **등록 시기**: 앱스토어 배포 직전 (Week 17~18)
- **발급처**: developer.apple.com → Certificates, Identifiers & Profiles → Keys
- **등록 위치**: `.env` + GitHub Secrets
- **주의**: `APPLE_PRIVATE_KEY`는 `.p8` 파일 내용을 줄바꿈 `\n` 이스케이프 후 입력

### 5. 네이버 로그인 키 (`NAVER_CLIENT_ID / NAVER_CLIENT_SECRET`)
- **등록 시기**: 소셜 로그인 구현 시 (Week 15~16)
- **발급처**: developers.naver.com → 애플리케이션 → 로그인 API 신청
- **등록 위치**: `.env` + GitHub Secrets

### 6. 네이버 블로그 검색 API (`NAVER_SEARCH_CLIENT_ID / NAVER_SEARCH_CLIENT_SECRET`)
- **등록 시기**: Phase 1 후반 — 트렌딩 장소 수집 파이프라인 구현 시
- **발급처**: developers.naver.com → 검색 API 신청 (네이버 로그인 앱과 동일 앱 가능)
- **등록 위치**: `ai/.env` + GitHub Secrets

### 7. EC2 배포 인프라 키 (`EC2_HOST / EC2_SSH_KEY`)
- **등록 시기**: EC2 서버 구성 시 (배포 준비 단계)
- **발급처**: AWS EC2 콘솔 → 인스턴스 생성 → 키 페어 생성
- **등록 위치**: GitHub Secrets 전용 (로컬 .env 에는 불필요)
- **주의**: `EC2_SSH_KEY`는 `.pem` 파일 전체 내용 붙여넣기

---

---

## 🟠 프로액티브 ↔ 챗봇 비일관성 (2026-07-29 실기기 확인 중 발견)

### ~~챗봇이 숙소를 모른다 — 프로액티브는 아는데~~ — 해결됨 (2026-07-29, `fix/chatbot-accommodation`)
- **해결 방법**: 도구를 4번째로 늘리지 않고, `get_route_status` 반환에 `accommodations`(전체 목록) + day별 `accommodation`을 얹고 `search_nearby_places`에 `origin: current|accommodation` 인자를 추가했다. 판단 규칙은 `_resolve_stay` 한 함수에 가뒀다(날짜 매칭 → 1곳 폴백(`date_matched=False`) → 그 외 `None`).
- **프롬프트도 같이 고쳐야 했다**: 코드만 고쳤을 때 모델이 숙소 이름은 알아냈는데 **검색을 아예 안 했다**(실측). `location_hint`의 "위치가 필요한 질문이면 되물어라"가 규칙 3을 이겼고, 오늘 날짜를 몰라 `today_day=null`이면 "여행 중이 아니시네요"라고 단정했다(마지막 일정이 끝난 밤 시간대에 항상 발생 — 숙소 질문이 가장 많이 나올 시간대가 정확히 거기다). `location_hint`에 숙소 예외를 명시하고, 시스템 프롬프트에 `오늘: {날짜} — 여행 N일차`를 추가해 해결했다.
- **검증**: "숙소 근처 카페 찾아줘" → 되묻지 않고 `"등록하신 남산힐호텔을 기준으로..."` + 장소 카드 5개. "우리 숙소 어디로 잡았더라?" → 이름·날짜를 답하고 **날짜가 여행 기간과 안 맞는 것까지 스스로 지적**. "근처 맛집 추천해줘"(숙소 언급 없음) → 기존대로 위치를 되묻는다(회귀 없음).
- **아래는 원래 기록** (조사 내용 보존)

#### (참고) 원래 기록 — 챗봇 숙소 미인지
- **관련 태스크**: 프로액티브 후속 수정 실기기 확인 (2026-07-29)
- **증상**: 챗봇에 "나 숙소 근처 카페 가고 싶어"라고 하면 **"지금 숙소가 어디에 있는지 알려주실 수 있을까요?"** 라고 되묻는다. 루트에 숙소가 등록돼 있는데도.
- **원인**: `ai/app/services/chat_service.py` 전체(510줄)에 `accommodation`/`숙소`/`stay` 문자열이 **한 번도 없다**(grep 0건). `_load_route`는 `routes` 단일 테이블만 보고, 시스템 프롬프트에도 숙소 자리가 없고, 도구 3종(`search_nearby_places`/`get_weather_forecast`/`get_route_status`) 어느 것도 숙소를 조회하지 못한다. `get_route_status`는 "루트 전체 개요"를 표방하지만 실제 SELECT는 `route_slots JOIN places`뿐이다. `search_nearby_places`는 입력 스키마가 `category_tags`·`radius_m`뿐이라 **"숙소 기준으로 찾아줘"를 표현할 방법 자체가 없고**, 기준 좌표 폴백은 `GPS → 추정 슬롯 → 도시 중심` 3단이라 숙소가 낄 자리가 없다.
- **되묻는 건 버그가 아니다**: 프롬프트 규칙 3번("추정도 없고 사용자가 위치를 말한 적도 없다면 되물어라")을 정확히 따른 결과다. 컨텍스트·도구 설계 누락이 원인이다.
- **비일관성이 핵심**: 같은 AI 서비스의 프로액티브는 숙소를 잘 조회한다 — `proactive_service._load_stay_distances`(`:358-375`)가 Day↔숙소 **날짜범위 조인**을 이미 구현했고 `far_from_stay` 진단에 쓴다. 사용자 입장에선 같은 앱인데 배너는 숙소를 알고 챗봇은 모른다.
- **선례가 있다**: `get_route_status`의 `is_today` 주입 주석 — "시스템 프롬프트 텍스트 힌트에만 의존하면 모델이 둘을 못 연결 짓고 사용자에게 며칠째인지 되묻는 문제가 있어(실측), 근거 데이터에 직접 반영한다". **지금 증상과 정확히 같은 실패 모드를 한 번 겪고 고친 기록**이다. 같은 처방을 숙소에 적용하면 된다.
- **해야 할 것**: ① `get_route_status` 반환에 Day별 숙소 추가(`_load_stay_distances`의 조인 재사용, `is_today` 주입과 같은 패턴) ② `search_nearby_places`에 기준점 인자 추가(`origin: current|accommodation`) — 이게 없으면 모델이 숙소를 알아도 그 근처를 검색할 방법이 없어 "검색한 척"하는 환각 위험이 생긴다 ③ 프롬프트 규칙 3번 보강
- **주의**: `proactive_service`가 `chat_service`를 import하므로(`:17`) 헬퍼는 **`chat_service`에 둬야** 순환 import를 피한다.
- **테스트 공백**: `ai/tests/` 전체에서 숙소 언급은 `test_route_service.py:389`의 주석 한 줄뿐. 챗봇 숙소 케이스가 전무하다.
- **우선순위**: 중간 — 기능 손실이고 사용자가 바로 체감한다

### ~~숙소 날짜가 여행 기간과 어긋난 데이터가 있다~~ — 확인 결과 **테스트 데이터**, 코드 문제 아님 (2026-07-29)
- **결론**: 입력 UI 버그가 아니다. 전수 조회 결과 숙소 5건 중 **4건이 루트 기간과 정확히 일치**한다.
  ```
   destination | 루트 기간              | 숙소 체크인~아웃      | 판정
   서울        | 2026-07-29 ~ 07-31    | 07-14 ~ 07-16        | ❌ ← 448c2939
   서울        | 2026-07-10 ~ 07-11    | 07-10 ~ 07-11        | ✅
   서울        | 2026-07-08 ~ 07-10    | 07-08 ~ 07-10        | ✅
   춘천        | 2026-07-08 ~ 07-10    | 07-08 ~ 07-10        | ✅ (2건)
  ```
- **어긋난 1건의 정체**: `448c2939`는 `created_at` 07-14 / `updated_at` **07-29 13:12** — 07-14에 만든 루트의 **날짜만 나중에 손으로 바꿨다**(프로액티브를 "여행 중" 상태로 만들어 테스트하려고). 숙소는 그대로 07-14에 머물렀다.
- **앱으로는 발생 불가**: `RouteController`의 `Mapping` 전수 확인 결과 **루트 날짜 수정 API 자체가 없다**(`/departure`·`/return`·`/visibility`·`/reorder`만 존재). 즉 사용자는 루트 날짜를 바꿀 수 없으므로 이 어긋남을 만들 수 없다.
- **조치**: 테스트 데이터 교정으로 끝. 아래 신규 항목이 이 조사에서 나온 실제 결함이다.

### ~~"근처" 검색이 조용히 50km까지 벌어진다 — 답변은 "근처"라고 말한다~~ — 해결됨 (2026-07-30)
- **해결**: 원인이 반경 확장이 아니라 **정렬**이었다. `ORDER BY RANDOM()`이라 50km 원 안에서 무작위로 뽑혀 5.1km와 27.3km가 나란히 올라왔다. `PostgisTagRetriever.sort`(`random`|`distance`, **기본 `random`**)를 추가해 챗봇 근처 검색·슬롯 대안만 거리순으로 바꿨다 — 아래 "주의"가 걱정한 루트 생성 영향은 **기본값을 기존 동작으로 둬서** 원천 차단했다(회귀 테스트 `test_default_sort_stays_random`). 반환에 `distance_m` + 서버가 계산한 `expanded`를 실어 모델이 "근처엔 없어서 조금 떨어진 곳"이라고 밝히게 했다. 실측: 남산힐호텔 기준 카페가 `2.4/2.5/2.6/2.8/2.8km`로 바뀌고 DB 직접 조회와 5곳 전부 일치 — **가장 가까운 카페는 원래 2.4km에 있었는데 27.3km(인천)가 대신 노출되고 있었다.** 계획: `docs/superpowers/plans/2026-07-30-chat-nearby-search-accuracy.md`
- **남은 것**: 아래 "근본 원인은 데이터 희소성"은 그대로 유효하다 — 위 「`#카페` 큐레이션 장소가 전국 313건뿐」 항목으로 옮겨 추적한다.
- **관련 태스크**: 챗봇 숙소 연동 검증 중 발견 (2026-07-29)
- **증상**: "숙소 근처 카페 찾아줘" → 답변은 "숙소 근처 카페를 찾았습니다"인데 실제 결과는 **연남동·강남 압구정·인천**이었다. 남산힐호텔에서 수십 km 떨어진 곳들이다.
- **원인**: `PostgisTagRetriever`가 후보 3건 미만이면 반경을 자동 확장한다(`ai/app/services/retrievers.py:76-77`, `후보 0건 — 반경 1500m → 50000m 확장` 로그로 확인). 남산힐호텔 1.5km 내 `#카페` 태그 장소가 `places`에 **0건**이라 곧바로 50km로 벌어졌다.
- **숙소 전용 문제가 아니다**: 현재 위치 기준 검색도 같은 리트리버를 쓰므로 동일하게 발생한다. 이번 숙소 연동이 만든 결함이 아니라 **기존 동작이 드러난 것**이다.
- **왜 문제인가**: 확장 사실이 도구 반환에 없어 모델이 알 방법이 없다 → "근처"라고 단정한다. 규칙 1(지어내지 마라)은 지켰지만 거리에 대해선 사실과 다른 답이 나간다.
- **해야 할 것**: 리트리버가 **실제 사용한 반경**(또는 확장 여부)을 반환하고, `search_nearby_places` 결과에 실어 모델이 "근처엔 없어서 조금 멀리까지 봤어요"라고 말하게 한다. 각 장소까지의 거리를 함께 주는 것도 방법.
- **주의**: `PostgisTagRetriever`는 **루트 생성에서도 쓴다** — 반환 형태를 바꾸면 그쪽 영향을 먼저 확인해야 한다. 그래서 이번 범위에서 뺐다.
- **근본 원인은 데이터 희소성**: 큐레이션 `places`가 특정 지역에 몰려 있다. 반경 확장은 그 증상을 가린 임시방편이다.
- **우선순위**: 중간 — 환각은 아니지만 사용자가 "근처"라는 말을 믿고 움직이면 손해를 본다

### (번호추가) 숙소 날짜를 루트 기간과 대조하지 않는다 (Spring 검증 누락)
- **관련 태스크**: 위 조사 중 발견 (2026-07-29)
- **증상**: `AccommodationService.create`(`:47-50`)는 `checkOutDate > checkInDate`만 검증하고 **루트의 `start_date`~`end_date`와 대조하지 않는다.** 따라서 앱으로도 여행 기간과 전혀 겹치지 않는 숙소를 저장할 수 있다.
- **왜 문제인가**: Day↔숙소 date-range 조인(`proactive_service._load_stay_distances`, 그리고 새로 만든 `chat_service._resolve_stay`)이 **조용히 빈 결과**를 낸다. 사용자는 숙소를 등록했는데 프로액티브도 챗봇도 모르는 상태가 되고, 어디가 잘못됐는지 알 방법이 없다.
- **완화는 돼 있음**: 챗봇 숙소 연동에서 "날짜로 못 정하면 숙소가 1곳뿐일 때 그걸 쓰되 `date_matched=false`로 표시" 폴백을 넣어 뒀다. 하지만 이건 증상 완화지 원인 수정이 아니고, 숙소가 여러 곳이면 여전히 못 찾는다.
- **해야 할 것**: `create`에서 `checkInDate < route.endDate && checkOutDate > route.startDate`(기간 겹침) 검증 추가. 완전 불일치면 `INVALID_INPUT`. 부분 겹침은 허용해야 한다(전날 입실 등 실제 사례가 있다).
- **주의**: 기존 데이터에 위반 건이 있는지 먼저 확인해야 한다 — 검증만 넣으면 조회는 멀쩡한데 수정 시 막히는 상태가 된다.
- **우선순위**: 중간 — 데이터 무결성이고, 챗봇·프로액티브 두 기능이 이 조인 위에 서 있다

### (번호추가) 항공편 시각을 루트 생성 단계에서 받지 않는다
- **관련 태스크**: 프로액티브 후속 수정 (2026-07-29)
- **현재 상태**: 가는 편/오는 편 시각이 루트 상세 화면의 작은 비행기 아이콘을 눌러야 나오는 선택 입력이라, 사용자가 존재를 모를 가능성이 높다.
- **위험**: 이 값이 없으면 `FLIGHT_DEPARTURE`·`RETURN_DEPARTURE` 두 규칙이 통째로 죽는다 — 프로액티브 9종 중 2종이 사실상 안 쓰인다.
- **해야 할 것**: 루트 생성 단계(날짜 고르는 화면)에서 "가는 편·오는 편 시각을 **아시면** 입력해주세요(선택)"로 받는다. 선택 입력 성격은 유지.
- **우선순위**: 중간 — 기능은 있는데 도달률이 낮다

## 🔵 데이터 파이프라인 보강 (TourAPI 수집 후속)

### (번호추가) TourAPI detailCommon2 상세 보강 수집
- **관련 태스크**: TourAPI 배치 수집기 구현 (2026-06-21 완료)
- **현재 상태**: `areaBasedList2`로 기본 좌표·주소·태그만 수집 (20,363건). 설명·영업시간·전화번호 미수집
- **해야 할 것**: `detailCommon2` 엔드포인트 병렬 호출 → `places.business_hours`, 상세 설명 컬럼 채우기
- **참고**: 일일 트래픽 1,000건 제한 → 배치 분할 실행 필요 (20,363건 / 1,000 = 21일 소요)

### (번호추가) places.business_hours · places.trend_score가 선언만 되고 비어 있음
- **관련 태스크**: 프로액티브 개입 엔진 구현 중 발견 (이슈 #143, 2026-07-27)
- **파일**: `backend/src/main/resources/db/migration/V2__create_places.sql`(컬럼 선언), `backend/src/main/java/com/cloumy/trip/entity/Place.java`(매핑에서 제외)
- **현재 상태**: `V2__create_places.sql`에 `business_hours JSONB`, `trend_score FLOAT`(주석: "네이버 블로그 API 기반 주 1회 갱신") 컬럼이 있으나, 두 컬럼에 값을 넣는 코드가 저장소 어디에도 없다. `Place.java`는 두 컬럼을 JPA 매핑에서 아예 제외했고("JPA 매핑 불가 → 네이티브 쿼리에서 직접 처리"), 프로젝트 전체에 `@Scheduled` 스케줄러 자체가 하나도 없다(전체 grep 결과 0건) — "주 1회 갱신" 주석은 구현되지 않은 계획일 뿐이다.
- **위험**: 라스트오더·영업시간·트렌드(웨이팅 근사) 기반 프로액티브 개입이 전부 불가능하다. 노션 프로액티브 시나리오 15개 중 ⑧혼잡도·⑫라스트오더가 이 데이터 부재에 막혀 있다(현재 구현된 프로액티브 규칙 8종에는 이 두 시나리오가 포함돼 있지 않음).
- **해야 할 것(논의 필요)**: "운영현실 데이터" 프로젝트(별도 트랙)의 선행 조건이다. ① `business_hours`는 위 "TourAPI detailCommon2 상세 보강 수집" 항목과 동일 작업(detailCommon2 병렬 호출)으로 함께 채울 수 있음 ② `trend_score`는 네이버 블로그 검색 API 기반 주 1회 갱신 배치를 신규로 구현해야 함(스케줄러 자체가 없어 실행 방식부터 결정 필요 — `@Scheduled` vs 별도 크론/배치 스크립트)
- **우선순위**: 중간 — 프로액티브 엔진 자체는 이미 정상 동작 중이고, 데이터가 붙으면 규칙만 추가하면 되는 구조라 지금 당장 막힌 기능은 없음

### (번호추가) places 테이블 유니크 제약 + upsert 전환
- **관련 태스크**: TourAPI 배치 수집기 구현 (2026-06-21 완료)
- **현재 상태**: places 테이블에 유니크 제약 없어 수집기 재실행 시 중복 INSERT 발생
- **해야 할 것**: Flyway 마이그레이션으로 `(source, name, location)` 유니크 제약 추가 → `ON CONFLICT DO UPDATE` 전환
- **우선순위**: 중간 (현재 시드 데이터는 1회 실행으로 충분, 정기 갱신 시 필요)

### ~~루트 추천 장소 풀이 100% TourAPI 단일 소스~~ — 네이버 지역검색 추가로 해결 (2026-07-05)
- **관련 태스크**: 숙소 입력(Accommodation) 기능 구현 중 발견 (2026-07-03)
- **당시 상태**: `places` 20,363건 전부 `source='tourapi'`. 카카오 보강 수집기(`collect_kakao.py`, 2026-06-29 실행)는 반경 150m 내 기존 TourAPI 장소와 겹쳐 신규 삽입 0건, 좌표 교정만 수행됨.
- **원인 분석**: 카카오의 도시 단위 넓은 카테고리/키워드 검색이 TourAPI와 마찬가지로 "잘 알려진 곳"부터 찾아내는 방식이라 반경 150m 중복 판정에 전부 걸림.
- **해결**: `ai/scripts/collect_naver_local.py` 신규 — 네이버 지역검색 API(`local.json`)를 도시 단위가 아니라 **동네 단위로 좁힌 키워드**("{동네} 로컬 맛집", "{동네} 노포" 등)로 검색해 카카오와 다른 결과를 확보. 전주/경주 2개 도시로 먼저 시험(순증 97/82건 확인) 후 전국 14개 도시로 확장, **총 1,180건 순증**(`source='naver'`, places 20,363→21,543건). 좌표는 `mapx/mapy ÷ 10^7 = WGS84`로 실측 검증됨. `places.source` CHECK 제약에 `'naver'` 추가(V10 마이그레이션).
- **한계**: `AREAS`의 동네 목록은 완전한 행정동 전수 조사가 아니라 잘 알려진 지역 샘플(도시당 5곳) — 더 세분화하면 추가 순증 여지 있음. 임베딩은 `generate_embeddings.py`로 별도 생성 필요.
- **Google Places / Klook / GetYourGuide / Viator / Amadeus Tours & Activities**: 검토했으나 이번 스코프에서 제외 — Klook/GetYourGuide/Viator/Amadeus는 셀프서비스 API 키 발급이 아니라 제휴 파트너 신청·계약(매출 쉐어) 절차가 필요한 B2B 구조라 코드 작업이 아니라 사업개발 성격. 자금 확보 후 재검토.
- **KOPIS(공연예술통합전산망)**: `milestones.md` Phase 0에 이미 계획됐으나 키 미발급 — 이번엔 보류, 추후 별도 진행.
- **크롤링 기반 콘텐츠(팝업스토어/전시/스포츠경기/대학축제/야시장/로컬체험)**: 공식 API 없음(크롤링 전제, ToS 리스크+유지보수 부담) + 이벤트는 "기간이 있는" 데이터라 `places`(영구 장소) 모델과 안 맞음(별도 테이블 필요) — 스코프 제외.

### (번호추가) `#카페` 큐레이션 장소가 전국 313건뿐 — 반경 검색이 사실상 못 걸린다
- **관련 태스크**: 챗봇 근처 검색 정확도 — 거리순 정렬 + 일정 장소 기준점 (2026-07-30)에서 원인 ①로 규명
- **현재 상태**: 태그별 큐레이션 장소 건수를 실측한 결과 편중이 심하다.

  | 태그 | 건수 |
  |---|---|
  | `#먹방` / `#식당` | 9,427 |
  | `#뷰맛집` / `#랜드마크` | 6,605 |
  | `#액티비티` | 2,443 |
  | `#쇼핑` | 726 |
  | **`#카페`** | **313** (서울 27건) |

- **증상**: 챗봇에 "근처 카페"를 물으면 **반경 1.5km 안 후보가 0건**이라 폴백이 걸린다 — 실제 진행 중 루트의 슬롯 6곳 전부 0건으로 실측됐다. `#먹방`도 9,427건이지만 공간 분포가 고르지 않아 기준점에 따라 1.5km 안이 2건 ~ 81건으로 흔들린다.
- **왜 지금 안 고치는가**: 이건 검색 코드 문제가 아니라 **수집 대상 선정 문제**다. 위 「TourAPI detailCommon2 상세 보강 수집」·「네이버 지역검색」과 같은 데이터 파이프라인 트랙이라 성격이 다르다. 2026-07-30 작업에서는 폴백이 걸렸을 때 **가까운 순으로 뽑고 거리를 밝히도록** 검색 로직만 고쳤다 — 데이터가 부족한 상태에서도 "인천·홍천이 근처로 나오는" 일은 없어졌지만, "1.5km 안에 카페가 있다"가 되려면 수집이 필요하다.
- **해야 할 것**: 카페 카테고리를 좁힌 수집기 추가. `ai/scripts/collect_naver_local.py`가 **동네 단위 좁은 키워드**로 순증 1,180건을 확보한 선례가 있으니("{동네} 로컬 맛집", "{동네} 노포") 같은 방식으로 "{동네} 카페", "{동네} 디저트", "{동네} 로스터리" 키워드를 돌리는 게 가장 싸다. 카카오 카테고리 검색(`CE7` = 카페)도 후보.
- **우선순위**: 중간 — 검색 로직 수정으로 "엉뚱한 곳이 나오는" 문제는 막았으나, 카페 추천 자체의 체감 품질은 데이터가 붙어야 올라간다

### TourAPI 축제 데이터에 기간(시작/종료일) 정보 없음
- **관련 태스크**: 데이터 소스 확장 검토 중 발견 (2026-07-05)
- **현재 상태**: `places`에 `#이벤트` 태그 407건이 TourAPI `areaBasedList2`(contentTypeId=15)로 이미 수집되고 있으나, `eventstartdate`/`eventenddate`를 반환하는 상세 엔드포인트(`detailIntro2` 또는 `searchFestival2`)를 안 써서 **시작/종료일 정보가 전혀 저장 안 됨**(`business_hours` 컬럼도 비어있음).
- **위험**: 몇 년 전에 끝난 축제도 영구 장소처럼 계속 루트 추천에 노출될 수 있음.
- **해야 할 것(논의 필요)**: `searchFestival2` 엔드포인트로 기간 정보 보강 수집, 또는 만료된 축제를 걸러낼 별도 이벤트 전용 테이블(시작일/종료일 컬럼) 설계 — `places`의 "영구 장소" 가정과 안 맞아서 스키마 변경 필요.
- **우선순위**: 낮음 — 지금도 루트가 완전히 깨지진 않음(그냥 오래된 축제가 추천될 뿐), 이벤트 전용 데이터 모델이 필요해지면 재검토.

---

## 🔵 챗봇 장소 추가 후속 (2026-07-30)

> **맥락**: 「챗봇 추천 장소를 루트에 추가」(7-29) → 「근처 검색 정확도」(7-30) → 「대화 명시 시 바로 추가」(7-30) 세 작업에서 남은 것.
> 설계 문서: `docs/superpowers/specs/2026-07-30-chat-direct-insert-design.md`, `docs/superpowers/plans/2026-07-30-chat-nearby-search-accuracy.md`

### (번호추가) 모든 걸 루트에 넣어야 하나 — 공유 설계와 함께 결정할 것
- **관련 태스크**: 대화로 자리를 명시하면 확인 없이 바로 추가 (2026-07-30)에서 사용자가 제기
- **질문**: *"자기 전에 숙소 근처 카페 가는 것도 루트에 반영을 해야 할까? 이것도 나중에 루트 공유가 되는 건가?"*
- **지금 구조**: 챗봇이 추천한 장소는 **`route_slots`에 들어가거나 아무 데도 안 남거나** 둘 중 하나다. 제3의 선택지가 없다.
- **왜 문제인가**:
  1. **일정의 성격이 다르다.** 「경복궁 가기 전에 카페」는 동선 위의 정거장이라 순서·시각이 의미 있고 `recomputeStartTimesForDay`가 다시 계산해야 맞다. 반면 「자기 전에 숙소 근처 카페」는 그날 밤 어딘가지 동선 위의 정거장이 아닌데, 지금은 똑같이 `order_index`를 받고 일정에 박힌다.
  2. **공유하면 남의 개인 사정이 딸려간다.** 「공유 루트 가져오기」가 다음 주제로 잡혀 있는데([[project_external_place_handling]] 메모리), 내가 묵는 호텔 옆 편의점까지 따라가면 공유물의 가치가 떨어진다.
- **검토한 방향**:

  | | 방향 | 대가 |
  |---|---|---|
  | A | 지금처럼 전부 루트에 (현행) | 단순함. 일정이 지저분해지고 공유 품질이 떨어짐 |
  | B | 슬롯에 성격 구분(일정/개인) 컬럼 추가 → 공유 시 개인 항목 제외 | 스키마 한 컬럼. 기존 화면 대부분 그대로 |
  | C | 루트와 별개인 **저장함**에 담고 나중에 일정으로 승격 | 새 화면·새 테이블. 가장 깔끔하지만 제일 큼 |

- **현재 판단**: **B가 지금 단계에 맞아 보인다** — `route_slots.is_pinned`가 이미 슬롯에 성격 플래그를 두는 선례이고, 숙소를 `accommodations` 별도 테이블로 뺀 것도 같은 발상이다. C는 「공유 루트 가져오기」·「미지의 장소 처리」까지 한꺼번에 설계해야 제값을 한다.
- **왜 지금 안 정하나**: **공유 기능의 모양이 정해져야 "무엇을 빼고 공유할지"가 정해지는데 아직 스펙이 없다.** 지금은 A(현행)로 두고 공유 설계 시 같이 브레인스토밍한다.
- **우선순위**: 🔵 「공유 루트 가져오기」 착수 시 **반드시 함께** 결정

### (번호추가) 챗봇이 도구를 안 부르고 세션 히스토리의 장소를 읊는다 (규칙 1 위반)
- **관련 태스크**: 챗봇 근처 검색 정확도 검증 중 발견 (2026-07-30)
- **파일**: `ai/app/services/chat_service.py` (`handle_chat` tool loop), Redis `chat:{user_id}:{route_id}` (TTL 2h)
- **증상**: 같은 세션에서 두 번째로 장소를 물으면 모델이 `search_nearby_places`를 **아예 호출하지 않고** 이전 턴에 나왔던 장소 이름·거리를 그대로 답변에 쓴다. 로그에 「장소 검색」 줄 없이 `삽입 자리 결정: hint={}`만 남는 것으로 확인됐다.
- **왜 문제인가**: ① 시스템 프롬프트 규칙 1("도구 호출 결과에 있는 장소만 사용하세요")을 정면으로 위반한다 ② `last_places`가 채워지지 않아 **`places: None` → 앱에 장소 카드가 아예 안 뜬다.** 답변 텍스트에는 장소가 있는데 카드가 없어 사용자가 탭할 수 없다 ③ 기준점이 바뀌었는데도 옛 결과를 재사용하니 거리가 틀릴 수 있다
- **재현**: 같은 루트로 장소 질문을 연달아 2~3회. 세션(`redis-cli DEL chat:...`)을 비우면 정상 호출된다.
- **해야 할 것(논의 필요)**: ① 히스토리에 어시스턴트 답변을 넣을 때 장소 이름을 남기지 않거나(요약만) ② 규칙 1을 "이전 답변에 나온 장소도 다시 도구로 확인하라"로 강화하거나 ③ 서버가 "장소를 언급했는데 `places`가 비었다"를 감지해 도구 호출을 강제하는 재시도. ③은 판단을 규칙에 두는 방향이라 이 프로젝트 원칙에 맞지만 왕복이 늘어 타임아웃과 상충한다.
- **우선순위**: 중간 — 첫 질문은 정상이고 세션이 2시간 TTL로 끊긴다. 하지만 "카드가 안 뜬다"는 사용자가 바로 체감하는 증상이다

### (번호추가) 챗봇 장소 카드 문구가 다시 단조로워짐 (Haiku 왕복 제거의 대가)
- **관련 태스크**: 챗봇 근처 검색 정확도 (2026-07-30)
- **파일**: `ai/app/services/retrievers.py` (`describe_candidate`)
- **현재 상태**: 카드 `reason`이 `describe_candidate`의 결정론적 문구("#카페 · 7.2km 거리")다. 2026-07-06에 **"다른 슬롯들의 풍부한 팁과 톤이 안 맞는다"는 사용자 피드백으로 Haiku 생성 방식으로 바꿨던 것을 되돌린 것**이다.
- **왜 되돌렸나**: Haiku 왕복이 한 요청의 LLM 호출을 3회로 만들어 Spring 15초 타임아웃을 자주 넘겼고, 그러면 카드가 아예 안 떴다. 제거 후 3.9~5.5초로 안정화됐다(사용자 승인).
- **해야 할 것(논의 필요)**: 왕복을 늘리지 않고 문구를 살리는 방법 — ① 메인 답변 생성 시 모델이 카드 문구까지 한 번에 만들게 하고 서버가 파싱(왕복 0회 추가) ② 카드 문구를 미리 계산해 `places`에 캐싱(장소별 1회) ③ Spring 타임아웃을 올리고 Haiku 왕복을 되살리기
- **우선순위**: 낮음 — 기능은 정상이고 거리 정보가 들어가 2026-07-06 당시보다는 낫다. 실사용에서 다시 걸리면 재검토

---

## 🔵 프로액티브 엔진 후속 (2026-07-29 코드 리뷰에서 남은 결함)

> **맥락**: 프로액티브 개입 엔진(#143)이 `main`에 머지된 뒤 진행한 코드 리뷰에서 **검증된 결함 10건**이 나왔다.
> 그중 6건은 「프로액티브 후속 수정 — 항공편 시각 정합성 + 활성 루트 선택」(2026-07-29, `feat/proactive-followup-fixes`)에서 처리했다.
> 여기 남은 것은 **그때 의도적으로 범위에서 뺀 4건 + 그 작업 중 새로 발견한 3건 = 7건**이다.
> 계획 문서: `docs/superpowers/plans/2026-07-29-proactive-followup-fixes.md` (「제외 (의도적)」 절)

**한눈에 보기**

| # | 항목 | 계층 | 한 줄 증상 | 우선순위 |
|---|---|---|---|---|
| 1 | `start_time` NULL 슬롯이 T2·T7을 죽임 | AI | 시간 미입력 슬롯 하나로 그 유저는 여행 내내 `DEPARTURE_SOON`·`FREE_GAP`을 못 받음 | 중간 |
| 2 | `FREE_GAP`이 현재 시각 미검사 | AI | 첫 일정 시작 전인데 "다음 일정까지 여유 있어요"가 뜨고 그날 개입 기회를 소모 | 중간 |
| 3 | 챗봇 자동 개입 가드가 세션 전역 | Frontend | 한 번 대화하면 그 앱 세션 동안 "먼저 말 걸기"가 영구히 안 됨 | 중간 |
| 4 | `proactiveContext` 무검증 시스템 프롬프트 삽입 | Spring + AI | 클라이언트 문자열이 system 역할로 들어가 신뢰 경계가 깨짐 | 중간 |
| 5 | 배너 X가 즉시 안 사라짐 | Frontend | 리렌더가 안 걸려 X를 눌러도 배너가 남음(재노출 방지 자체는 정상) | 낮음 |
| 6 | 오는 편이 `end_date` 다음날 새벽이면 미발동 | AI | 그 시각 phase가 `out_of_range`라 `RETURN_DEPARTURE`가 평가조차 안 됨 | 낮음 |
| 7 | `createdAt`도 `LocalDateTime` 매핑 | Spring | `departureAt`과 같은 타입 불일치 — 표시 전용이라 체감 버그는 없음 | 낮음 |

> **묶어서 처리하기 좋은 단위**: 1·2·6은 전부 `ai/app/services/proactive_service.py` 한 파일이고 단위 테스트로 검증 가능하다.
> 3·5는 프론트 프로액티브 UI 한 덩어리다. 4는 성격이 보안이라 단독으로 다루는 게 맞다.

### ~~start_time이 NULL인 슬롯 하나가 T2·T7을 통째로 죽인다~~ — 확인 결과 **정상 동작**, 코드 변경 없음 (2026-07-29)
2026-07-29 코드 리뷰가 결함으로 지적했고 이 문서에도 한때 "`_current_and_next`가 `start_time` 있는 첫 슬롯을 찾도록 수정"이라고 적었으나 **둘 다 오답이었다.** 같은 오판이 반복되지 않도록 근거를 남긴다.

- **지적 내용**: `proactive_service._load_slots`는 `start_time` 필터 없이 전 슬롯을 가져오는데 `chat_service._estimate_current_slot`은 `start_time IS NOT NULL`만 본다. `_current_and_next`가 next를 `today_slots[idx+1]` 위치로 고르므로 그 자리가 시간 미입력 슬롯이면 T2·T7이 스킵된다.
- **왜 결함이 아닌가 ① — 도달 경로가 없다**: `start_time`이 NULL이 되는 경로는 Java 5곳뿐인데 모두 Day 전체가 NULL이거나 맨 뒤다. 수동 작성 루트(`createManualSlots`)와 그 clone은 Day 전체 NULL → `_estimate_current_slot`이 빈 목록을 받아 `confidence: "low"` 반환 → T2·T6·T7이 애초에 스킵된다. 섞이는 유일한 경로인 미적용 고정 슬롯은 `order_index`가 임시값 `100_000+`이라 항상 그날 맨 뒤로 정렬된다. 지적이 예시로 든 "중간에 NULL이 끼는" 상태를 만드는 코드 경로가 없다.
- **왜 결함이 아닌가 ② — 건너뛰면 오히려 틀린다**: `transport_minutes`는 '그 슬롯에서 **바로 다음** 슬롯까지'의 이동시간이다. `[10:00 A(→B 10분), (NULL) B, 14:00 C]`에서 B를 건너뛰고 C를 next로 잡으면 `leave_by = 14:00 - 10분 = 13:50`이 되는데 그 10분은 A→B 값이라 B를 거쳐 가는 시간이 통째로 빠진다. **틀린 시각으로 알림을 띄우느니 안 띄우는 게 낫다** — 지금의 FFE #8 가드가 그 판단이다.
- **`_load_slots`에 필터를 넣으면 안 되는 이유**: 이 목록은 시각과 무관한 규칙도 함께 쓴다 — `_rule_empty_day` 슬롯 카운트, `_rule_weather_alert` 실외 카운트, P1의 `packed_day`/`long_walk`. 필터를 넣으면 수동 루트에서 EMPTY_DAY가 오발동하고 `outdoorCount`가 0이 돼 T4가 스킵된다.
- **대신 한 것**: `_current_and_next` 회귀 테스트 3종 추가(이 함수는 테스트가 하나도 없었다) + `_current_and_next` 독스트링·`_load_slots` 주석에 위 근거 명시.

### ~~FREE_GAP이 현재 시각을 안 봐서 미래 공백을 "지금 여유"로 안내한다~~ — 해결됨 (2026-07-29, `d241581`)
`_rule_free_gap`에 `current_end <= now < next_start` 가드를 넣고 `gapMinutes`를 `next_start - now`(남은 여유)로 바꿨다. 가드만 넣으면 공백 한가운데서 이미 지나간 시간까지 세는 문제가 남는데, 문구가 "다음 일정까지 {{gapMinutes}}분 여유가 있어요"라 남은 시간을 말해야 맞다. 회귀 테스트 3종 추가.

### ~~챗봇 자동 개입이 한 번 대화하면 그 세션 내내 막힌다~~ — 해결됨 (2026-07-29)
대화를 루트에 귀속시켜 해결했다. `setActiveRouteId`가 루트 **변경 시에만** `messages`·`pendingProactive`를 비우고(같은 routeId 재호출은 쿼리 refetch라 건드리면 안 된다), `seedFromProactive`가 `routeId`를 받아 `activeRouteId`를 함께 확정한다 — 후자가 없으면 홈에서 배너를 탭할 때 `activeRouteId`가 아직 null이라, 챗 마운트 시 `null → routeId`가 '루트 변경'으로 판정돼 배너가 방금 넣은 말풍선을 지운다.

가드(`messages.length > 0`)는 그대로 뒀다. messages가 루트에 귀속되면 "이 루트의 대화가 진행 중인가"라는 정확한 의미가 되기 때문이다. 가드의 두 목적 중 "배너 탭 케이스 거르기"는 원래부터 `isDismissedToday`와 중복이었다(배너가 `seedFromProactive`보다 먼저 `dismissToday`를 찍는다).

**의도적으로 남긴 구멍**: 같은 루트·같은 앱 세션에서 사용자가 먼저 말을 건 뒤에는 자동 개입이 안 뜬다. Zustand에 persist가 없어 앱 재시작 시 초기화되고 홈 배너는 독립적으로 계속 뜨므로 개입 자체가 사라지진 않는다. "대화 중 끼어들지 않는다"는 원래 의도를 지키는 쪽이다.

**미완**: 실기기 확인 5항목 — 특히 배너 탭 → 챗 진입 시 말풍선이 정확히 1개인지.

### ~~proactiveContext가 검증 없이 시스템 프롬프트에 삽입된다~~ — 해결됨 (2026-07-29)
클라이언트가 완성된 문장을 보내던 것을 폐기하고 `type` + `params`만 받아 **서버가 AI용 서술을 조립**하도록 바꿨다(`chat_service._build_proactive_descriptor`). 검증은 FastAPI의 태그드 유니온(`schemas.ProactiveContext`, 9종)이 맡고 Spring은 `type` 화이트리스트만 보는 얇은 가드다 — `params` 스키마를 Java에 복제하면 규칙 추가 시 변경 지점이 3배가 된다.

핵심은 **자유 문자열(`placeName`/`destination`/`nextPlaceName`)을 서술에서 아예 제외**한 것이다. `type`+`params`로 바꾸기만 하면 그 필드들이 여전히 주입 통로로 남는다(길이를 25자로 제한해도 지시문이 들어간다). 숫자·열거·시각만 넣어 통로 자체를 없앴다. 검증: `placeName`에 지시문을 넣어도 챗봇이 따르지 않고 응답·로그 어디에도 나타나지 않음.

사용자에게 보이는 4개 언어 문구는 여전히 앱 `proactiveText.ts`가 만든다("판단은 규칙이, 표현은 앱이" 유지).

### (번호추가) 배너 X가 같은 화면에 머무는 동안 사라지지 않는다
- **관련 태스크**: 2026-07-29 코드 리뷰
- **파일**: `frontend/components/route/ProactiveBanner.tsx`
- **현재 상태**: `handleDismiss`가 `dismissToday`(MMKV 쓰기)와 `sendProactiveFeedback`(fire-and-forget)만 호출한다. 둘 다 React 상태나 쿼리 캐시를 안 건드려 리렌더가 발생하지 않는데, `isDismissedToday` 검사는 렌더 시점에만 수행된다.
- **증상**: X를 눌러도 배너가 남아 있고, 다시 탭하면 같은 말풍선이 챗봇에 중복으로 쌓인다. 화면을 벗어났다 오면(언마운트/리마운트) 정상 동작하므로 **재노출 방지 자체는 정상**이다(2026-07-27 실기기 검증 기록 참고).
- **해야 할 것**: 로컬 `dismissed` 상태를 두거나 `queryClient.setQueryData(['proactive', routeId], null)`로 리렌더를 유발한다.
- **우선순위**: 낮음 — 기능 손실이 아닌 UX 결함

### (번호추가) 오는 편이 여행 종료일 다음날 새벽이면 RETURN_DEPARTURE가 안 뜬다
- **관련 태스크**: 프로액티브 후속 수정 (2026-07-29)
- **파일**: `ai/app/services/proactive_service.py` (`_trip_phase`)
- **현재 상태**: `_trip_phase`는 `start_date - 1`(pre_trip)과 `start_date ~ end_date`(during)만 인정한다. 가는 편 새벽 항공편은 `FLIGHT_DEPARTURE`를 `_RULES_PRE_TRIP`에 등록해 해결했지만, 오는 편이 `end_date` **다음날** 새벽이면 그 시각의 phase가 `out_of_range`라 규칙이 평가되지 않는다.
- **위험**: 낮다. 사용자가 그 날을 `end_date`로 잡는 게 자연스러워 실제 발생 빈도가 낮다.
- **해야 할 것(논의 필요)**: `_trip_phase`에 `post_trip`을 추가하거나 `end_date + 1`까지 during으로 넓히는 안. 다만 `_trip_phase`는 **비용 방어의 핵심 경로**다(FFE #1 — 대부분의 호출이 여기서 쿼리 1개로 끝난다). 범위를 넓히면 모든 유저의 여행 종료 다음날 호출이 스냅샷 수집까지 진행된다. 이득 대비 비용을 먼저 따져야 한다.
- **우선순위**: 낮음

### (번호추가) `createdAt`이 TIMESTAMPTZ인데 Java는 LocalDateTime으로 매핑
- **관련 태스크**: 프로액티브 후속 수정 (2026-07-29) — `departureAt`을 `OffsetDateTime`으로 고치면서 발견
- **파일**: `backend/.../dto/RouteListResponse.java:15`, `backend/.../common/entity/BaseEntity.java`, **그리고 `BaseEntity`를 상속하지 않고 직접 선언한 곳** — `trip/entity/Bookmark.java:38`, `trip/entity/Expense.java:58`, `trip/dto/ExpenseResponse.java:13` (2026-07-30 전수 확인)
- **현재 상태**: `created_at`/`updated_at` 컬럼은 `TIMESTAMPTZ`인데 Java는 오프셋 없는 `LocalDateTime`으로 받는다. `departureAt`과 정확히 같은 구조의 불일치다. `Route` 엔티티는 이미 `OffsetDateTime`이라 해당 없음.
- **위험**: 낮다. `createdAt`은 표시 전용이고 프론트가 이 값을 서버로 되돌려 보내지 않아 왕복 누적 오차가 없다. 또 `LocalDateTimeSerializer`는 `InstantSerializerBase`를 상속하지 않아 `spring.jackson.time-zone` 영향도 안 받는다.
- **해야 할 것**: `OffsetDateTime`으로 통일. 다만 `RouteListResponse`는 목록·커뮤니티·탐색·클론·수동생성 5개 경로가 공유하므로 회귀 범위가 넓다.
- **우선순위**: 낮음 — 체감 버그가 없다. 다른 이유로 `RouteListResponse`를 손댈 때 함께 처리하는 게 합리적이다.

---

## 🔵 이동시간 기능 후속 (Tmap 대중교통 연동 후속 — 2026-07-04)

### (번호추가) 탭하면 상세 노선 + 실시간 도착정보
- **관련 태스크**: 이동수단별 이동시간 반영 (2026-07-04 완료)
- **현재 상태**: 슬롯 사이 이동시간(도보/자동차 근사치 + 대중교통 Tmap API)만 표시. "몇 번 버스/몇 호선"이나 "3분 후 도착" 같은 상세·실시간 정보는 없음
- **해야 할 것**: 경로 상세(노선/환승)는 Tmap 대중교통 API 응답의 `legs`를 추가로 파싱하면 됨. 실시간 도착 카운트다운은 별도 데이터소스 필요(서울은 서울 열린데이터광장 지하철/버스 실시간 API, 타 지역은 지자체별로 다름 — 전국 커버 시 국가교통정보센터 TAGO 통합 고려) + "탭할 때마다 재조회"하는 온디맨드 엔드포인트 신설 필요(정적 계산 불가)
- **우선순위**: 낮음 — 외국인 관광객 핵심 pain point는 "뭘 타야 할지 모름"이지 실시간 정밀도가 아님(한국관광공사 조사 기준)

### (번호추가) 해외 목적지 확장 시 이동시간 provider 분기
- **관련 태스크**: 이동수단별 이동시간 반영 (2026-07-04 완료)
- **현재 상태**: `transport_service.py`가 Tmap(한국 전용)만 지원. 구글맵은 한국에서 도보 길찾기 자체가 안 되지만(정밀지도 반출 규제), 해외에서는 정상 동작함
- **해야 할 것**: 해외 도시 지원 시 목적지 국가에 따라 provider를 Tmap→Google Maps로 분기하는 로직 추가. 지금은 100% 국내 전용이라 시기상조(YAGNI)
- **우선순위**: 낮음 — 해외 확장이 확정되면 재검토

### (번호추가) 다일차 루트 생성 중 day 경계 추적 꼬임 재발 관찰
- **관련 태스크**: 이동수단별 이동시간 반영 검증 중 발견 (2026-07-04)
- **현재 상태**: `route_service.py`가 Claude 스트리밍 중 place_id 오류로 인한 자기교정(출력 재시작)을 처리하는데, 재시작이 여러 번 겹치면 마지막 day의 `day_buffer`가 불완전한 상태로 flush되는 사례를 실제로 관찰함(대구 2박 생성 테스트에서 day2 슬롯의 이동시간 필드가 전부 비어있었음 — 슬롯 자체는 저장됐으나 enrichment 대상에서 빠짐).
- **비슷한 기존 이슈**: `milestones.md`의 "다일차 루트 생성 안정성 버그 3건"(2026-07-02 완료 처리됨)과 같은 종류로 보이나 완전히 해결되지 않은 것으로 보임
- **해야 할 것**: 재현 케이스 확보 후 `_process_line`/`_ingest`의 day 경계 판정 로직 재검토 필요. 이번 태스크 범위 밖이라 별도 조사 필요
- **우선순위**: 중간 — 드물게 발생하고 크래시는 아니지만(슬롯 자체는 저장됨) 데이터 정합성 문제

### (번호추가) `replaceSlot()`도 이동수단 미지정 시 이동정보 누락 (동일 버그)
- **관련 태스크**: 챗봇 추천 장소 삽입 시 이동수단 미적용 버그 수정 (2026-07-06 완료)
- **현재 상태**: `RouteSlotService.insertSlotAfter()`는 `route.transportMode`가 null이면 "car" 기본값으로 대체하도록 고쳤지만(2026-07-06), 기존 Pin&Reshuffle 대안 교체 기능인 `replaceSlot()`(198~200번째 줄)에는 완전히 동일한 `if (route.getTransportMode() != null && ...)` 가드가 그대로 남아있어 같은 문제가 잠재해 있음 — 이동수단 미지정 루트에서 슬롯을 대안으로 교체하면 이동정보가 안 붙음
- **해야 할 것**: `insertSlotAfter()`와 동일하게 `DEFAULT_TRANSPORT_MODE("car")` 폴백 적용
- **우선순위**: 낮음 — 이번 버그 리포트는 챗봇 삽입 기능 한정이었고, replaceSlot의 실제 체감 빈도는 별도 확인 필요

---

## 🔵 숙소 기능 후속 (다중 숙소 지원 — 2026-07-04)

### (번호추가) 날짜 구간별 숙소가 다른 경우 지원
- **관련 태스크**: 숙소 입력 + 이동시간 정확도 개선 (2026-07-04 완료) — 실사용 피드백으로 발견
- **현재 상태**: 숙소는 "여행당 1건"만 지원. `accommodations` 테이블 자체는 `check_in_date`/`check_out_date` 구간을 가진 레코드 구조라 여러 건 저장이 스키마상 불가능하진 않지만, 프론트 입력 UI·AI TSP 앵커 로직(`tsp_service.py`)·Spring 저장 로직 전부 "숙소 1개가 여행 시작~종료일 전체를 커버"한다고 전제하고 구현돼 있음
- **해야 할 것(논의 필요)**:
  - 프론트: 숙소 입력 UI를 "여행당 1개" → "날짜 구간별 여러 개 추가" 폼으로 변경
  - AI: TSP 앵커가 지금은 "하루 buffer 전체에 앵커 좌표 1개"를 전제하는데, day별로 해당 day가 속한 숙소 구간을 찾아 다른 앵커 좌표를 매핑하도록 변경
  - Spring: 숙소 저장/조회 API를 리스트 기반으로 변경, day → 숙소 매핑 로직 추가
- **우선순위**: 낮음 — 지금은 "숙소 1건"으로도 대부분의 국내 여행(1~2박, 동일 지역 숙박) 케이스는 커버됨. 여러 도시를 이동하는 장기 여행(3박 이상, 지역 이동형) 비중이 늘면 재검토

---

## 🔵 폴백 루트 후속 (폴백 — 유사 루트 추천 구현 후속 — 2026-07-04)

### (번호추가) 폴백 여부 DB 영속화 + 프론트 배지 표시
- **관련 태스크**: 폴백 — 유사 루트 추천 (Spring, 2026-07-04 완료)
- **현재 상태**: FastAPI 장애 시 DB 유사 루트를 찾아 SSE로 흘려보낼 때 각 라인에 `is_fallback: true`를 실어 보내지만, `route_slots`/`routes` 테이블에는 영속화하지 않음(휘발성 플래그). 프론트도 이 필드를 아직 소비하지 않음(`frontend/lib/api/routes.ts`의 `RouteSlot` 타입에 없음)
- **해야 할 것(논의 필요)**: 재조회(`GET /v1/routes/{routeId}`) 시에도 "이 루트는 유사 루트에서 가져온 결과입니다" 같은 안내를 보여주려면 `routes` 테이블에 `is_fallback` 컬럼 추가(Flyway 마이그레이션) + 프론트 타입/UI 배지 추가 필요
- **우선순위**: 낮음 — 폴백 자체는 정상 동작하며, 이 항목은 UX 개선(사용자가 "왜 이 루트가 나왔는지" 인지하는 것)에 관한 것

---

## 🔵 장소 다국어화 후속 (장소 데이터 다국어화 결정·구현 — 2026-07-10)

### (번호추가) 전체 21,525건 배치 번역 실행 보류
- **관련 태스크**: 장소 데이터 다국어화 방식 결정·구현 (2026-07-10 완료)
- **현재 상태**: 스키마(V14 마이그레이션)·배치 스크립트(`ai/scripts/translate_places.py`)·실시간 번역+캐시(AI `/ai/places/translate` + `PlaceService.getPlaceDetail`) 전부 구현·검증 완료(소량 20건 실전 테스트 성공). 나머지 21,525건은 스크립트만 있고 미실행
- **해야 할 것**: `cd ai && python -m scripts.translate_places` (인자 없이) 실행
- **참고**: Haiku 기준 예상 비용 ~$14, 소요시간 45~70분(세마포어 5 동시성), 재실행 안전(`name_en IS NULL` 재조회)
- **우선순위**: 낮음 — 사용자가 원할 때 실행

### (번호추가) 번역 데이터가 API 응답에 미노출
- **관련 태스크**: 장소 데이터 다국어화 구현 (2026-07-10)
- **현재 상태**: `places.name_en/ja/zh_hans/zh_hant` 등 8개 컬럼에 번역이 저장되지만, `PlaceDetailResponse` 등 어떤 API 응답도 이 필드를 반환하지 않음. 백엔드에 `Accept-Language` 헤더 처리 로직 자체가 없음
- **해야 할 것(논의 필요)**: 요청 로케일을 어디서 판단할지(헤더 vs 유저 프로필 언어 설정), 어느 응답(place detail·route slot·검색 결과)까지 로케일 적용 범위로 볼지 결정 필요 — 결정 후 DTO 확장
- **우선순위**: 중간 — 데이터는 쌓이는데 실제로 쓰이는 곳이 아직 없어, 이게 없으면 이번 기능 전체가 "저장만 하고 안 쓰는" 상태로 남음

### (번호추가) 배치 번역 우선순위의 "역" 태그 근사치
- **관련 태스크**: 배치 번역 스크립트 구현 (2026-07-10)
- **현재 상태**: Notion 결정에 "관광지·역·주요 시설부터" 우선순위가 명시됐으나, `category_tags` 어휘에 "역(전철역/기차역)"에 대응하는 태그가 없어 `#랜드마크/#관광명소/#실내/#역사` 4개만으로 우선순위 근사
- **해야 할 것**: 역 관련 태그 체계가 생기면(또는 이름에 "역"/"驛" 매칭 등 별도 규칙) 우선순위 쿼리 갱신
- **우선순위**: 낮음 — 배치는 결국 전체를 다 처리하므로 순서 문제일 뿐, 최종 결과에는 영향 없음

---

## 🟠 테마 태그 어휘 불일치 (탐색 탭 구현 중 발견 — 2026-07-10)

### (번호추가) step-2.tsx THEMES(9종)와 실제 places.category_tags 어휘가 다름 — 6/9가 사실상 무필터
- **관련 태스크**: 탐색 탭(장소 브라우징+북마크) 구현 중 발견. 페르소나 태그 시스템, route 생성, 예산 자동조정(BudgetSettingsService) 전부에 영향을 주는 **더 큰 범위의 사전 존재 이슈**
- **현재 상태**: 실제 DB에 있는 `category_tags` 값은 `#식당(9427)·#먹방(9427)·#랜드마크(6605)·#뷰맛집(6605)·#액티비티(2443)·#실내(1273)·#역사(1273)·#쇼핑(726)·#이벤트(407)·#핫플(349)·#카페(313)` 11종뿐이다. `frontend/app/route/create/step-2.tsx`의 `THEMES`(맛집·카페·관광·자연·쇼핑·문화·액티비티·힐링·야경 9종)와 비교하면 **카페·쇼핑·액티비티 3개만 정확히 일치**하고 나머지 6개(맛집·관광·자연·문화·힐링·야경)는 DB에 그 이름의 태그가 아예 없음
- **왜 지금까지 안 터졌나**: `ai/app/services/retrievers.py`의 `PostgisTagRetriever`가 태그 필터로 3건 미만이 나오면 반경을 50km로 확장하고, 그래도 3건 미만이면 **태그 필터 자체를 조용히 제거하고 재시도**하는 폴백이 있어서 루트 생성은 에러 없이 항상 결과를 반환한다. 즉 유저가 "자연"·"야경" 등을 골라도 실제로는 필터링 없이 전체 후보 중에서 뽑히고 있었을 가능성이 높다 — 기능은 안 깨지지만 유저 의도(테마 선택)가 조용히 무시됨
- **같이 영향받는 곳**: `backend/.../trip/service/BudgetSettingsService.java`의 `TAG_BOOSTS`(관광·문화 키 포함), `backend/.../trip/service/PersonaTagAutoAssignService.java`의 `PERSONA_THEMES`(자연·야경·문화·관광 포함) — 전부 실제로 매칭될 확률이 낮은 태그 문자열을 키로 쓰고 있음
- **이번엔 탐색 탭만 임시 대응**: 탐색 탭의 필터 칩은 `THEMES`를 재사용하지 않고 **실제 11종 태그를 직접** 보여주도록 구현함(`ExploreController`/`browse.tsx`). 페르소나 기본필터 매핑도 탐색 탭 전용으로 별도 작성(`K_FOOD_LOVER→먹방/식당`, `CAFE_HOPPER→카페`, `SHOPPING_MAVEN→쇼핑`, `CULTURE_EXPLORER→역사/랜드마크`, `NIGHT_OWL→핫플`(근사치 — Night Owl은 원래 "야간 활동 선호" 의미인데 핫플은 시간대 무관 "요즘 뜨는 곳"이라 완전히 일치하진 않음, 나중에 재검토), `NATURE_SEEKER`는 대응 태그 없어 매핑 안 함
- **해야 할 것 — 2026-07-10 논의로 방향은 잡혔으나 실행은 보류(사용자가 외국인 관광객 대상 자체 리서치로 카테고리 최종 확정 후 진행 예정)**:
  - **2축 설계로 합의**: 축1 "장소 유형"(맛집←식당+먹방+뷰맛집, 카페←카페, 관광←랜드마크+역사, 쇼핑←쇼핑, 액티비티←액티비티, 실내←실내 — 6종, 지금 데이터로 바로 가능) + 축2 "분위기/특성 modifier"(다른 카테고리와 교차 결합 가능한 배지, 카테고리 버튼과 별도)
  - **자연 vs 액티비티는 별개 축**: 액티비티=활동 축("뭘 하는가", 실내 클라이밍장도 포함), 자연=환경 축("어떤 장소인가", 조용한 전망대도 포함) — 등산·해수욕장처럼 겹치는 경우도 있지만 합치지 않고 분리 유지하기로 함
  - **핫플 반영 방식 결정**: 별도 필터 모드가 아니라 기존 `is_hidden_gem`(💎) 배지와 동일한 패턴으로 카드에 🔥 배지로 얹기로 함 — 이미 검증된 UI 패턴 재사용이 필터 토글보다 자연스럽다고 판단. `PlaceBrowseCard.tsx`에 `isHiddenGem` 배지 옆에 `isHotPlace` 배지 추가하면 됨(필터 쿼리 자체는 안 바꿔도 됨, 카드 표시만 추가)
  - **이벤트는 배지보다 별도 섹션이 맞지만 보류**: 기간 한정 성격이라 "진행중 이벤트"로 보여줘야 의미가 있는데, 시작/종료일 데이터가 없다는 기존 이슈(아래 "TourAPI 축제 데이터에 기간 정보 없음" 항목)가 선행돼야 함. 지금은 핫플과 동일하게 배지로만 우선 반영
  - **자연/힐링(업종) 태그 확보 방법**: TourAPI `contentTypeId=12`(관광지)가 `cat1`(대분류: A01=자연 vs A02=인문)을 무시하고 무조건 `#뷰맛집,#랜드마크`로 고정 매핑 중이라(`ai/scripts/collect_tourapi.py:36-43`) `cat1=A01`만 추려도 `#자연` 태그 확보 가능 — cat3까지 갈 필요 없이 cat1만으로 충분할 것으로 보이나 실제 응답 스키마 확인 필요. 힐링은 두 갈래로 나뉨: **업종으로서의 힐링(마사지·스파·온천·찜질방)**은 레포츠(28) cat2/cat3 세분화로 자연과 동일한 방식으로 확보 가능해 보이나 미검증. **분위기로서의 힐링(조용하고 편안한 느낌)**은 리뷰·설명 텍스트 감성 분석 등 완전히 다른 방법이 필요해 이번 논의 범위 밖
  - **야경**: 원본 어디에도 시간대/야간 신호가 없음. 뷰맛집이 제일 가깝지만 "경치 좋은 식당"이라 식당 한정 의미라 대체 불가
  - **다음 순서(사용자 지정)**: 사용자가 외국인 관광객 대상 리서치로 최종 카테고리 확정 → 그 다음 TourAPI cat1(+가능하면 cat2/cat3) 캡처하도록 재수집 스크립트 수정 → 전체 재수집 실행 → `BudgetSettingsService.TAG_BOOSTS`/`PersonaTagAutoAssignService.PERSONA_THEMES`/`step-2.tsx` THEMES/탐색 탭 필터 전부 확정된 카테고리로 갱신
- **우선순위**: 중간 — 지금 당장 뭔가 깨지진 않지만(폴백이 있어서), 유저가 고른 테마가 실제로는 절반 이상 무의미하게 무시되고 있다는 건 핵심 기능(AI 루트 생성 + 예산 자동조정 + 페르소나 자동추가)의 체감 품질에 영향을 줌

---

## 🔵 탐색 탭 장소 정보 보강 (사진 + 한줄 요약 — 2026-07-10)

### (번호추가) 탐색 탭 장소에 사진 + 한줄 요약 추가 예정
- **관련 태스크**: 탐색 탭(장소 브라우징+북마크) 구현 후 논의. 장소마다 사진을 나중에 추가할 예정인데, 그와 짝을 이룰 "이 장소가 어떤 곳인지" 한줄 요약도 필요하다고 판단
- **현재 상태**: 후보로 검토했던 두 방법 모두 지금 당장 쓸 데이터가 없음을 확인
  - **AI 루트 tip 재사용은 부적합**: `route_slots.tips`는 장소 자체 속성이 아니라 특정 루트/일정 문맥에서 AI가 생성한 코멘트라 같은 장소도 루트마다 다른 tip을 가질 수 있음. 실제 DB 확인 결과 tip이 있는 슬롯 42건 중 서로 다른 장소는 31개뿐(전체 21,545개 중), 그마저 일부는 장소당 tip이 2개씩 달라 "이 장소의" 한줄로 쓸 정답이 없음
  - **사용자 리뷰 요약은 방향은 맞지만 콜드스타트**: 장소 자체에 대한 일반적 정보라 성격은 더 맞지만, 리뷰 작성 UI·저장 구조·AI 요약 파이프라인이 전부 없고 리뷰 데이터도 0건이라 지금 당장 구현해도 보여줄 게 없음
- **해야 할 것(논의된 방향)**: 두 소스를 섞는 하이브리드로 결정
  1. TourAPI 출처 장소(`places.source = 'tourapi'`)는 `detailCommon2`(현재 미사용 — 수집 스크립트가 `areaBasedList2` 목록 API만 호출해 소개문 필드 자체를 안 받아옴, `ai/scripts/collect_tourapi.py`)에 이미 관광공사가 작성한 소개문이 있어 이걸 한줄로 다듬어 사용(품질 제일 좋음, 단 추가 API 호출 필요 — 21,545건 개별 상세 조회라 배치 규모 있음)
  2. 소개문이 없는 출처(카카오/히든젬)는 이름+주소+category_tags 기반으로 AI가 짧게 생성 — `translate_places.py`/`PlaceService.getPlaceDetail`의 실시간 캐시 패턴(배치로 기존 건 일괄 채우고, 신규 장소는 첫 조회 시 실시간 생성+캐시)을 그대로 재사용 가능
- **다음 순서**: 사진 추가 작업과 묶어서 "장소 정보 보강" 하나의 기능으로 나중에 정식 브레인스토밍 예정(사용자 지정)
- **우선순위**: 낮음 — 사진 기능 착수 시점에 함께 진행

---

## 🔵 페르소나 태그 시스템 후속 (취향 태그 시스템 재설계 — 2026-07-10)

### (번호추가) 횟수 기반 성취 칭호 시스템
- **관련 태스크**: 취향 태그 시스템 재설계 (10종 페르소나, 2026-07-10 완료) 논의 중 제안됨
- **아이디어**: 지금 만든 10종 페르소나 태그(K-food Lover 등, "무엇을 좋아하는가")와는 별개로, "얼마나 했는가"를 세는 횟수 기반 성취 칭호(예: "10회 다녀온 전문가 여행가") 부여
- **현재 상태**: 컨셉만 논의됨, 스키마·임계값·표시 방식 전부 미정. `hidden_gems` 후속의 `user_levels` 테이블(자금 확보 후 구현 섹션)과 결이 비슷해 보이지만 GPS 인증 기반이 아니라 일반 루트 생성 횟수 기반이라 별개 트랙
- **해야 할 것(논의 필요)**: 카운트 대상(루트 생성 수? 특정 태그 조합? 도시 수?), 칭호 단계(1단계만 vs 레벨), 페르소나 태그와 같은 자리에 표시할지 별도 UI로 분리할지
- **우선순위**: 낮음 — 구체화 전 단계, 나중에 별도 브레인스토밍 필요

### (번호추가) 인증된 요청이 정의되지 않은 경로로 가면 404 대신 500 반환
- **관련 태스크**: 취향 태그 시스템 검증 중 발견 (2026-07-10) — 제거한 구 엔드포인트(`PUT /v1/users/me/persona-tags`) 검증하다가 우연히 발견, 페르소나 태그 기능과 무관한 전역 이슈
- **현재 상태**: JWT 인증은 통과했지만 매핑되지 않은 경로로 요청하면 `NoResourceFoundException`이 전역 예외 핸들러에서 `Exception` 캐치에 걸려 500 `INTERNAL_ERROR`로 응답됨(정상은 404). 인증 자체가 실패하는 경로는 401이 정상 반환됨(필터 단계라 무관)
- **위험**: 클라이언트가 오타 난 API 경로를 호출해도 "서버 내부 오류"로 보여 디버깅에 혼선을 줌. 심각한 보안 이슈는 아님
- **해야 할 것**: 전역 예외 핸들러에 `NoResourceFoundException` → 404 매핑 추가
- **우선순위**: 낮음 — 기능 자체는 정상 동작, UX/디버깅 편의 이슈

---

## 🟣 자금 확보 후 구현 (모두의 창업 지원금 이후)

### 8. Hidden Gems 기능 전체
- **연기 이유**: 모두의 창업 1차 지원금 미확보 → GPS 인증 기반 커뮤니티 구축 비용 부담
- **구현 범위**:
  - GPS 인증 기반 현지 발견 장소 등록 (photo + 좌표 인증)
  - 희소성 점수 알고리즘 (`rarity_score`) 운영
  - Hidden Gem 배지 자동 부여/해제 + FCM 알림 ("핫플이 됐어요 🔥")
  - `user_levels` 테이블 기반 레벨 시스템
- **단, 데이터 모델은 유지**: `places.rarity_score`, `places.is_hidden_gem`, `hidden_gems` 테이블은 그대로 두고 로직만 비활성화
- **해외에서 더 중요**: 언어 장벽 + 정보 비대칭 환경에서 Hidden Gems 가치가 국내보다 높음 → 해외 피벗 시 우선 적용

---

*마지막 업데이트: 2026-07-27*
